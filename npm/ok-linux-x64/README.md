# @openkeyring/ok-linux-x64

Bundled Open-Keyring `ok` binary for Linux x86_64 (glibc 2.35+).

This package is installed as an optional dependency of `@openkeyring/ok`. It does not download binaries during installation.
