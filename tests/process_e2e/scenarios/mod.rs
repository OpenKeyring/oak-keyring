// E2E scenario modules
// Each scenario module tests a specific end-to-end user workflow

mod create_vault;
mod first_run;
// mod terminal_cleanup; // Disabled: Esc key exit behavior is broken, unrelated to current fixes
mod unlock;
